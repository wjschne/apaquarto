\documentclass[
$if(documentmode)$
  $documentmode$,
$endif$
$if(floatsintext)$
  floatsintext,
$endif$
  longtable,
$if(a4paper)$
  a4paper,
$endif$
  nolmodern,
  notxfonts,
  notimes,
$if(nofontenc)$
  nofontenc,
$endif$
$if(suppress-title-introduction)$
  donotrepeattitle,
$endif$
$if(biblatex)$
  biblatex,
$endif$
$if(notab)$
  notab,
$endif$
$if(helv)$
  helv,
$endif$
$if(nosf)$
  nosf,
$endif$
$if(tt)$
  tt,
$endif$
$if(draftfirst)$
  draftfirst,
$endif$
$if(draftall)$
  draftall,
$endif$
$if(mask)$
  mask,
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}


